# MCP Servers Service Dependencies

# Web Framework
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# MCP Protocol (exposes FastAPI endpoints as MCP tools)
fastapi-mcp>=0.4.0

# HTTP Client
# NOTE: FastAPI/Starlette TestClient is not yet compatible with httpx>=0.28
httpx>=0.25.0,<0.28

# MongoDB (async)
motor>=3.3.0

# Math helpers (similarity)
numpy>=1.26.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

