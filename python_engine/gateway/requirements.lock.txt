# Pinned dependencies for reproducible builds.
#
# Why this file exists:
# - `requirements.txt` uses version ranges for flexibility during development.
# - Docker/CI deployments benefit from deterministic, repeatable resolution.
#
# How to use:
#   pip install -r requirements.lock.txt
#
# Python: 3.11 (matches Docker base image)

fastapi==0.124.2
uvicorn[standard]==0.38.0
httpx==0.28.1
pydantic==2.12.5
pydantic-settings==2.12.0
python-dotenv==1.2.1
python-multipart==0.0.20
